# 更新日志

本文件记录各版本的变更。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

> 发版流程：改 `pyproject.toml` 的 `version` → 在本文件补一节 → 打 tag `vX.Y.Z` 推上去。
> 工作流会把对应小节抽出来当 GitHub Release 正文，应用内的更新弹窗渲染的也是这段文字。

## [0.1.3] - 2026-09-13

### 新增

- **扫码登录**。「设置 → 账号」点「扫码登录」，用手机 B 站客户端扫码即可拿到 Cookie，
  自动保存并显示昵称与头像，不必再去浏览器的开发者工具里翻。账号卡按登录状态切换：
  未登录给「手动填写」与「扫码登录」两条路，已登录只剩「退出登录」。
- **Cookie 状态灯真的反映能不能用**。主页此前只问「填没填」，被吊销的 Cookie 照样显示
  「已配置」；现在进主页会真检测一次，失效变红并提示去设置页换一个。结论有 7 天信任期，
  期内不重复请求（失效只信 30 分钟——风控下那个接口会假报没登录，判红判错了不能钉一周）。
  Cookie 确认有效后会在后台静默拉取全部表情包，切到表情包页时列表已经就绪。
- **直播间专属表情**。表情包页新增第三个标签：输入直播间房间号，拉取该房间的主播专属表情，
  网格预览、点开看大图，整个房间作为一个下载项加入下载队列（目录按「主播名 [房间号]」命名）。

### 修复

- **代理开关不再被自动打开**（上次没修干净）。0.1.2 把这条配置迁移改成按标记只跑一次，
  但迁移本身的前提——「配置里留着代理地址就说明用户在用代理」——只在最早那个还没有开关的
  版本上成立；此后用户关掉开关并不会清空地址，残留地址一直躺在配置里，任何一次配置回落
  （文件被删或改坏、卸载重装保留 `%APPDATA%`）都会把它翻回「开」。现在这条迁移**直接删除**，
  代理一律默认关，要用时自己拨一次（地址仍在框里，一步操作）。
- **非布尔值的配置不再被读成「开」**。此前用组件库的 `BoolValidator`，它把非法值一律兜成
  `True`；配置文件里出现 `"proxyEnabled": "false"` / `null` 这类值时，代理开关会是打开的。
- **设置页的下载设置改完即生效**。下载目录、线程数、代理不再需要点一次「保存」；
  手动保存会让界面与文件有机会不一致（在框里清空了值、文件里却留着旧的），
  那正是上面那个开关反复回弹的源头。直接关窗（点 X / Alt+F4）时未失焦的改动也会补写。

## [0.1.2] - 2026-08-31

### 新增

- **GIF 标识与动图预览**。表情包卡片与单个表情的左下角新增「GIF」角标，是不是动图包一眼可辨；
  表情详情网格鼠标悬浮即播放动图，离开恢复静态图；图片查看器打开动图即自动播放，翻页自动切换。
- **启动开屏面板**。应用启动时短暂显示带图标、名称与版本号的开屏面板，
  不再在主窗口构造的几秒里一片空白。

### 修复

- **手动关掉的代理开关不会再被自动打开**。配置迁移改为按 schema 标记只跑一次，
  此前在部分机器上会出现用户关闭代理后，应用一重启开关又回到「开」的现象。

### 优化

- **主页更跟手**。展示图改为预烘焙后的一次直绘，滚动平滑步数减半；
  此前一格滚轮被摊成 24 帧，主页单帧重绘十几毫秒时必掉帧，看着像「滑不动」。

## [0.1.1] - 2026-08-31

### 新增

- **加载失败可以重新加载**。图片网格与全屏查看器里右键 →「重新加载」；加载失败的缩略图
  会直接亮出一个「↻」按钮，收藏集的「视频加载失败」提示也改成点一下就重试。
  此前失败一次就得退出重开整个应用才有机会重来。
- 发布工作流支持在 Actions 页手动触发，不打 tag 也能验证编译与打包是否正常。

### 修复

- **图片查看器的翻页与关闭按钮**。翻页按钮从图片左右两个极边挪到底部信息行
  （`‹ 名称 3 / 20 ›`），首尾自动禁用；关闭按钮改为贴在图片右上角跟着图片走，
  并固定为白图标 + 半透明深色圆底 —— 原先亮色主题下是黑图标压在黑色遮罩上，
  窗口一大就找不到、甚至看不见。
- **设置页「下载加速」卡展开后收不起来**。收起动画取到了过期的滚动条范围，
  高度降不回标题行那一格。

## [0.1.0] - 2026-08-30

首个公开版本。

### 新增

- **主页**：应用介绍、状态概览、三张功能入口卡、快速上手 / 关于 / 最近搜索。
- **表情包页**：按包 ID 查询、登录后拉取全部表情包并按关键词过滤，包详情预览与下载，
  支持 GIF 动图。
- **收藏集页**：关键词搜索 B 站装扮 / 收藏集，详情页分「静态图片」与「动态视频」两个
  标签，图片可点开全屏查看器翻页，视频可内嵌播放。
- **下载页**：会话级混合队列（表情包 + 收藏集），批量下载、逐项进度与结果统计，
  全部成功的项目自动出队。
- **设置页**：Cookie、下载目录、代理（含连通性测试）、下载线程数、缓存上限、
  主题与字体渲染后端。
- 搜索框浮层历史面板；缩略图与接口响应落盘缓存，可设上限并一键清除。
- 亮色 / 暗色主题，侧栏一键切换；内置 LXGW 文楷等宽字体。
- 应用内检查更新：对比 GitHub Release 版本号，展示更新说明并可下载安装，
  支持 GitHub 下载加速镜像与安装包 SHA-256 校验。

### 说明

- 所有接口来自 B 站公开 API，可能随官方更新失效；仅供学习交流。
- 部分功能（全部表情包、收藏集下载）需要在设置页填写 B 站 Cookie。
